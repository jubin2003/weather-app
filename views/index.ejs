<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Weather Monitoring</title>
    <link rel="stylesheet" href="/styles/style.css">
</head>
<body>
    <h1>Recent Weather Data</h1>
    <table>
        <tr>
            <th>City</th>
            <th>Main Condition</th>
            <th>Temperature (°C)</th>
            <th>Feels Like (°C)</th>
            <th>Timestamp</th>
        </tr>
        <% weatherData.forEach(data => { %>
        <tr>
            <td><%= data.city %></td>
            <td><%= data.main_condition %></td>
            <td><%= data.temperature.toFixed(2) %></td>
            <td><%= data.feels_like.toFixed(2) %></td>
            <td><%= new Date(data.timestamp).toLocaleString() %></td>
        </tr>
        <% }) %>
    </table>
    <a href="/summary">View Daily Summaries</a>
</body>
</html>
